use bevy::app::App;
use bevy::prelude::Plugin;

pub struct MilitaryPlugin;

impl Plugin for MilitaryPlugin {
    fn build(&self, _app: &mut App) {
        // TODO HIGH: Add the "reload" and "shooting" logic here.
    }
}
