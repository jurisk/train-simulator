# We did not get it running as Fly.io has some specific requirements for UDP:
#   https://fly.io/docs/networking/udp-and-tcp/
# Created automatically by flyctl
# fly.toml app configuration file generated for train-simulator on 2024-06-09T18:09:46+03:00
#
# See https://fly.io/docs/reference/configuration/ for information about how to use this file.
#

app = 'train-simulator'
primary_region = 'waw'

[build]

[[services]]
  internal_port = 5000
  protocol = "udp"

  [[services.ports]]
  port = "5000"

[http_service]
  internal_port = 8080
  force_https = true
  auto_stop_machines = true
  auto_start_machines = true
  min_machines_running = 0
  processes = ['app']

[[vm]]
  memory = '1gb'
  cpu_kind = 'shared'
  cpus = 1
